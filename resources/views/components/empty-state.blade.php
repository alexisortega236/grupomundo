@props(['title' => 'No hay resultados', 'text' => 'Ajusta los filtros o vuelve a intentarlo.'])
<div class="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center">
    <p class="text-xl font-extrabold text-[#082142]">{{ $title }}</p>
    <p class="mt-2 text-slate-500">{{ $text }}</p>
</div>
