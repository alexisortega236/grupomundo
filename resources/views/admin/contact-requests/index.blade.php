<x-admin-layout title="Solicitudes de contacto"><form class="mb-4"><select name="status" class="rounded border-[#ded8ca]"><option value="">Todos los estados</option><option value="new" @selected(request('status') === 'new')>Nuevas</option><option value="contacted" @selected(request('status') === 'contacted')>Contactadas</option><option value="closed" @selected(request('status') === 'closed')>Cerradas</option></select><button class="rounded bg-[#0d2723] px-4 py-2 text-white">Filtrar</button></form><div class="rounded-lg bg-white p-4">@forelse($requests as $r)<a class="block break-words border-b py-3" href="{{ route('admin.contact-requests.show',$r) }}">{{ $r->name }} · {{ $r->phone }} · {{ $r->status->label() }}</a>@empty<p class="text-sm text-[#687773]">No hay solicitudes para mostrar.</p>@endforelse</div>{{ $requests->links() }}</x-admin-layout>
