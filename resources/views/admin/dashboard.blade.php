<x-admin-layout title="Dashboard">
    <div class="flex items-center justify-between"><h1 class="text-3xl font-black text-[#082142]">Dashboard</h1><a href="{{ route('admin.properties.create') }}" class="rounded-full bg-[#d3aa56] px-5 py-3 font-bold text-[#082142]">Crear propiedad</a></div>
    <div class="mt-8 grid gap-4 md:grid-cols-4">@foreach($stats as $label => $value)<div class="rounded-2xl bg-white p-5 shadow-sm"><p class="text-sm text-slate-500">{{ $label }}</p><p class="mt-2 text-3xl font-black text-[#082142]">{{ $value }}</p></div>@endforeach</div>
    <div class="mt-8 grid gap-6 lg:grid-cols-2"><section class="rounded-2xl bg-white p-6"><h2 class="font-black text-[#082142]">Últimas propiedades</h2><div class="mt-4 grid gap-3">@foreach($latestProperties as $property)<a class="flex justify-between border-b py-2" href="{{ route('admin.properties.edit', $property) }}"><span>{{ $property->title }}</span><span>{{ $property->status->label() }}</span></a>@endforeach</div></section><section class="rounded-2xl bg-white p-6"><h2 class="font-black text-[#082142]">Últimas solicitudes</h2><div class="mt-4 grid gap-3">@foreach($latestRequests as $request)<a class="flex justify-between border-b py-2" href="{{ route('admin.contact-requests.show', $request) }}"><span>{{ $request->name }}</span><span>{{ $request->status->label() }}</span></a>@endforeach</div></section></div>
</x-admin-layout>
