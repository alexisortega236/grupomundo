<x-admin-layout title="Usuarios"><a class="mb-4 inline-block rounded bg-[#d5b673] px-4 py-2" href="{{ route('admin.users.create') }}">Crear usuario</a><div class="rounded-lg bg-white p-4">@forelse($users as $user)<div class="flex flex-wrap justify-between gap-2 border-b py-3"><span class="break-all">{{ $user->name }} · {{ $user->email }} · {{ $user->roleLabel() }}</span><a href="{{ route('admin.users.edit',$user) }}">Editar</a></div>@empty<p class="text-sm text-[#687773]">No hay usuarios para mostrar.</p>@endforelse</div>{{ $users->links() }}</x-admin-layout>
