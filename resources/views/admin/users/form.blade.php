<x-admin-layout :title="$user->exists ? 'Editar usuario' : 'Crear usuario'"><form method="POST" action="{{ $user->exists ? route('admin.users.update',$user) : route('admin.users.store') }}" class="rounded-lg bg-white p-6">@csrf @if($user->exists)@method('PUT')@endif<div class="grid gap-4 md:grid-cols-2"><x-form.input label="Nombre" name="name" :value="$user->name"/><x-form.input label="Correo" name="email" type="email" :value="$user->email"/><x-form.input label="Contrasena" name="password" type="password"/><x-form.select label="Rol" name="role" :value="$user->role" :options="['admin'=>'Admin','editor'=>'Editor']"/></div><button class="mt-5 rounded bg-[#0d2723] px-5 py-3 text-white">Guardar</button></form></x-admin-layout>
